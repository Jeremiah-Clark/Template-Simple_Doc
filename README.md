# Template-SimpleDoc
A template system that allows simple documents to be written in pure GitHub-Flavored Markdown then built into consistently styled PDFs. Metadata and styling options are set in a master YAML file, allowing for easy, basic customizability.
